from setuptools import setup

setup(name='dalcinj_dsnd_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dalcinj_dsnd_distributions'],
      author='Jhonston',
      zip_safe=False)
